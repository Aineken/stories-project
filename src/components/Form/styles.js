import { createTheme } from "@mui/material";

export default createTheme({
  form: {
    display: "flex",
    flexWrap: "wrap",
    justifyContent: "center",
    gap: "10px",
    padding: "10px",
  },
  fileInput: {
    width: "100%",
  },
  buttonSubmit: {
    // marginBottom: "10px",
  },
});
