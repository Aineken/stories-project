import { createTheme } from "@mui/material/styles";

export default createTheme({
  mainContainer: {
    display: "flex",
    alignItems: "center",
  },
  smMargin: {
    // margin: theme.spacing(1),
    margin: "30px 0",
  },
});
