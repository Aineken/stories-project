import { createTheme } from "@mui/material";

export default createTheme({
  appBar: {
    borderRadius: "10px",
    margin: "30px 0",
    display: "flex",
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center",
  },
});
